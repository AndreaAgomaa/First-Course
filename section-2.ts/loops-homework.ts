export let base = 10;
let limit = 10;

let i = 0;

//for (let i = 0; i <= limit; i++) {
//  console.log(base, "*", i, "=", base * i);
//}

while (i <= limit) {
  console.log(base, "*", i, "=", base * i);

  i++;
}
